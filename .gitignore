# Terraform state contains secrets in plaintext (admin token). Never commit.
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log

# Real variable values.
terraform.tfvars
*.auto.tfvars
!terraform.tfvars.example

# Raw admin token (only its Argon2 hash belongs in tfvars). Never commit.
.admin-token
*.secret

override.tf
override.tf.json
*_override.tf
*_override.tf.json

.DS_Store

# Leftovers from the removed Rust checkout
target/
*qr.png